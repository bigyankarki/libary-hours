{
  "name": "library",
  "version": "1.0.0",
  "description": "get library hours straight from command line",
  "main": "library.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "command",
    "line"
  ],
  "author": "Bigyan Karki",
  "license": "ISC",
  "dependencies": {
    "request": "^2.83.0",
    "yargs": "^10.0.3"
  }
}
